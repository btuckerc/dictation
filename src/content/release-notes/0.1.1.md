A compact Fn setup row, a quieter recording indicator, and consistent Dictation branding.

- Shows only when either dictation shortcut is exactly Fn/function.
- Links directly to Keyboard Settings and explains the global Fn action change.
- Keeps the emoji picker available with Control-Command-Space.
- Shrinks the recording pill to a waveform-only capsule, without the dot or X button. Escape still cancels.
- Uses an opaque black pill with fine, round-ended accent-colored strokes and restrained entrance/exit motion.
- Gives each stroke more independent spectral movement, with lower speech bands arranged around the center and a small shared voice level reaching the edges. When VAD is enabled, its current speech decision suppresses non-speech movement without adding another inference pass.
- Uses a lightly springy rise and slower, cushioned release instead of snapping down between syllables. Animation sleeps when settled and respects reduced motion without re-rendering the transcript on every microphone update.
- Keeps the pill steady on shortcut release: a travelling dot pulse replaces the flashing transcription/processing label.
- Keeps the menu bar menu to Copy Last Transcript, Settings, and Quit.
- Adds a verified macOS install command that preserves the signing identity and launches the installed bundle through macOS rather than a terminal child process.
- Uses Dictation for the executable, logs, product labels, and project links while preserving existing settings, history, and upstream credits.
- Keeps the original blue and replaces the other accent presets with white, electric yellow, and hot pink. The Fn waveform follows the selected color; light-theme outlines and selected navigation retain readable contrast.
